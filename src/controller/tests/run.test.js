describe('Run', () => {
    let sandbox = null;
    let mockController = null;

    before(() => {
        sandbox = sinon.createSandbox();

    });

    it('init should called correctly', () => {

    });
});